{
  "name": "root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^8.1.9",
    "prettier": "^3.4.1"
  },
  "scripts": {
    "dev": "lerna run dev",
    "start": "lerna run start",
    "build": "lerna run build --scope frontend",
    "test": "lerna run test",
    "diff": "lerna diff"
  },
  "dependencies": {
    "react-hook-form": "^7.54.0",
    "zod": "^3.24.1"
  }
}
